<nav class="navbar navbar-expand-lg bg-primary py-3" data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('app_home') }}"><img style="width:80px; position:absolute;top:-10px;left:-2px" src="{{ asset('images/logo.png') }}" alt="logo  appRecette"></a>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto " style="margin-left: 180px">
				<li class="nav-item mx-5 ">
					<a class="nav-link " id="home" href="{{ path('app_home')}}">Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link " id="list" href="{{ path('task_index')}}">To Do List</a>
				</li>


			</ul>

			{% if app.user %}
				<div class="nav-item px-4">


					<a class="dropdown-item" href="{{ path('security_logout') }}">Deconnexion</a>
				</div>
			</div>
		{% else %}
			<div class="d-flex">
				<a class="btn btn-primary" href="{{ path('security_login')}}">Connexion</a>
				<a class="btn btn-primary" href="{{ path('security_registration') }}">Inscription</a>

			</div>

		{% endif %}

	</div>
</div></nav>
